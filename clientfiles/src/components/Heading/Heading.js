import React from 'react';

import classes from './Heading.module.css';

const heading = (props) => {
    return (
        <h1 className={classes.Heading}>Bookmark your Favorite Quote!</h1>
    );
}

export default heading;